# ============================================================
# requirements.agent1.txt
# Only for Agent 1 — Environmental Intelligence
# Adds ~4-5 GB on top of the base layer (TF + GEE + geo stack)
# ============================================================

# Base dependencies (inherited via multi-stage build)
-r requirements.base.txt

# Google Earth Engine
earthengine-api==0.1.401
geemap==0.33.2
google-auth==2.30.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0

# Deep Learning (CNN flood model — flood_fast_best.keras)
tensorflow-cpu==2.16.1

# ML & scientific
scikit-learn==1.5.0
scipy==1.13.1
pillow==10.3.0
rasterio==1.3.10
geopandas==0.14.4
matplotlib==3.9.0
tqdm==4.66.4
joblib==1.4.2

# Social media (Twitter / X)
tweepy==4.14.0

# OpenAI (LLM enrichment of social media posts)
openai==1.30.1
